{
  "name": "cortex-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Cortex backend: finds related sources with Exa and ranks them with an LLM via OpenRouter.",
  "type": "module",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "dev": "node --env-file=.env dev-server.js",
    "dev:vercel": "vercel dev",
    "deploy": "vercel --prod",
    "test:related": "bash ./test-related.sh"
  },
  "dependencies": {
    "exa-js": "^1.5.0",
    "openai": "^4.77.0",
    "vercel": "^39.0.0"
  }
}
